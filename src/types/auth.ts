type LoginForm = {
    userId: string
    password: string
}

type UserData = {
    userId: string
    password: string
    email: string
    phoneNumber: string
    companyName: string
}

export type {LoginForm, UserData}
